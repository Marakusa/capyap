<Page
    x:Class="CapYap.Views.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:CapYap.Helpers"
    xmlns:local="clr-namespace:CapYap.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="SettingsPage"
    d:DataContext="{d:DesignInstance local:SettingsPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    <Page.Resources>
        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
    </Page.Resources>

    <StackPanel>
        <TextBlock
            FontSize="20"
            FontWeight="Medium"
            Text="General Settings" />
        <TextBlock Margin="0,12,0,0" Text="Theme" />
        <ui:ToggleSwitch x:Name="AutoStartToggle"
            Margin="0,12,0,0"
            Command="{Binding ViewModel.ChangeAutoStartCommand, Mode=OneWay}"
            CommandParameter="{Binding IsChecked,RelativeSource={RelativeSource Self}}"
            Content="Open CapYap on startup"
            IsChecked="{Binding ViewModel.AutoStart, Mode=OneWay}" />


        <TextBlock
            FontSize="20"
            FontWeight="Medium"
            Text="Personalization"
            Margin="0,32,0,0"/>
        <TextBlock Margin="0,12,0,0" Text="Theme" />
        <RadioButton
            Margin="0,12,0,0"
            Command="{Binding ViewModel.ChangeThemeCommand, Mode=OneWay}"
            CommandParameter="theme_light"
            Content="Light"
            GroupName="themeSelect"
            IsChecked="{Binding ViewModel.CurrentTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Light, Mode=OneWay}" />
        <RadioButton
            Margin="0,8,0,0"
            Command="{Binding ViewModel.ChangeThemeCommand, Mode=OneWay}"
            CommandParameter="theme_dark"
            Content="Dark"
            GroupName="themeSelect"
            IsChecked="{Binding ViewModel.CurrentTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Dark, Mode=OneWay}" />

        <TextBlock
            Margin="0,32,0,0"
            FontSize="20"
            FontWeight="Medium"
            Text="About CapYap" />
        <TextBlock Margin="0,12,0,0" Text="{Binding ViewModel.AppVersion, Mode=OneWay}" />

        <ui:Button x:Name="QuitButton" Margin="0,32,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="&#xE8BB;" 
                           FontFamily="Segoe Fluent Icons"
                           FontSize="16"
                           VerticalAlignment="Center"
                           Margin="0,0,4,0"/>
                <TextBlock Text="Quit CapYap" VerticalAlignment="Center"/>
            </StackPanel>
        </ui:Button>
    </StackPanel>
</Page>
