<ui:FluentWindow
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CapYap.Views.Windows"
    xmlns:custom="clr-namespace:LoadingSpinnerControl;assembly=LoadingSpinnerControl"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    x:Class="CapYap.Views.Windows.MainWindow"
    Title="{Binding ViewModel.ApplicationTitle, Mode=OneWay}"
    Width="1100"
    Height="650"
    MinWidth="1150"
    MinHeight="600"
    d:DataContext="{d:DesignInstance {x:Type local:MainWindow}, IsDesignTimeCreatable=True}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{ui:ThemeResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"
    ExtendsContentIntoTitleBar="True"
    Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <ui:FluentWindow.InputBindings>
        <KeyBinding
            Key="F"
            Command="{Binding FocusCommand, ElementName=AutoSuggestBox}"
            Modifiers="Control" />
    </ui:FluentWindow.InputBindings>

    <Grid>
        <ui:TitleBar
            x:Name="TitleBar"
            Title="{Binding ViewModel.ApplicationTitle}"
            Grid.Row="0"
            CloseWindowByDoubleClickOnIcon="True">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="/Views/Windows/icon.png" />
            </ui:TitleBar.Icon>
            <ui:TitleBar.TrailingContent>
                <ui:Image 
                    x:Name="AccountButton" 
                    HorizontalAlignment="Right" 
                    Margin="0,0,12,0"
                    Width="38" 
                    Height="38"
                    Source="/Assets/avatar.png" 
                    Stretch="UniformToFill">
                    <ui:Image.Clip>
                        <EllipseGeometry Center="19,19" RadiusX="19" RadiusY="19"/>
                    </ui:Image.Clip>
                </ui:Image>
            </ui:TitleBar.TrailingContent>
        </ui:TitleBar>

        <ui:NavigationView
            x:Name="RootNavigation"
            Padding="42,0,42,0"
            BreadcrumbBar="{Binding ElementName=BreadcrumbBar}"
            FooterMenuItemsSource="{Binding ViewModel.FooterMenuItems, Mode=OneWay}"
            FrameMargin="0"
            IsBackButtonVisible="Visible"
            IsPaneToggleVisible="True"
            MenuItemsSource="{Binding ViewModel.MenuItems, Mode=OneWay}"
            OpenPaneLength="310"
            PaneDisplayMode="Left"
            TitleBar="{Binding ElementName=TitleBar, Mode=OneWay}">
            <ui:NavigationView.Header>
                <ui:BreadcrumbBar x:Name="BreadcrumbBar" Margin="42,32,42,20" />
            </ui:NavigationView.Header>
            <ui:NavigationView.ContentOverlay>
                <Grid>
                    <ui:SnackbarPresenter x:Name="SnackbarPresenter" />
                </Grid>
            </ui:NavigationView.ContentOverlay>
        </ui:NavigationView>


        <Grid x:Name="PreviewPanel" Background="#CC000000" Visibility="Hidden">
            <Canvas x:Name="PreviewCanvas">
                <Image x:Name="PreviewImageComponent"
                    RenderTransformOrigin="0.5,0.5"
                    Stretch="Uniform">
                    <Image.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="PreviewImageScale" ScaleX="1" ScaleY="1"/>
                            <TranslateTransform x:Name="PreviewImageTranslate" X="0" Y="0"/>
                        </TransformGroup>
                    </Image.RenderTransform>
                </Image>
            </Canvas>
            <StackPanel Margin="0,408,0,8" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center" Height="36">
                <ui:Button x:Name="SaveButton" Margin="2">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE896;" 
                           FontFamily="Segoe Fluent Icons"
                           FontSize="16"
                           VerticalAlignment="Center"
                           Margin="0,0,4,0"/>
                        <TextBlock Text="Save" VerticalAlignment="Center"/>
                    </StackPanel>
                </ui:Button>
                <ui:Button x:Name="ShareButton" Margin="2">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72D;" 
                           FontFamily="Segoe Fluent Icons"
                           FontSize="16"
                           VerticalAlignment="Center"
                           Margin="0,0,4,0"/>
                        <TextBlock Text="Share" VerticalAlignment="Center"/>
                    </StackPanel>
                </ui:Button>
                <ui:Button x:Name="DeleteButton" Margin="2">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE74D;" 
                           FontFamily="Segoe Fluent Icons"
                           FontSize="16"
                           VerticalAlignment="Center"
                           Margin="0,0,4,0"/>
                        <TextBlock Text="Delete" VerticalAlignment="Center"/>
                    </StackPanel>
                </ui:Button>
                <TextBlock Text="&#xE052;" 
                    FontFamily="Segoe Fluent Icons"
                    FontSize="16"
                    VerticalAlignment="Center"
                    Margin="12,0,4,0"/>
                <TextBlock x:Name="ViewsText" Text="0" VerticalAlignment="Center"/>
                <TextBlock Text="&#xEA35;" 
                    FontFamily="Segoe Fluent Icons"
                    FontSize="16"
                    VerticalAlignment="Center"
                    Margin="12,0,4,0"/>
                <TextBlock x:Name="SizeText" Text="0 B" VerticalAlignment="Center"/>
                <ui:Button x:Name="SetAsAvatarButton" Margin="14,2,2,2">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE13D;" 
                           FontFamily="Segoe Fluent Icons"
                           FontSize="16"
                           VerticalAlignment="Center"
                           Margin="0,0,4,0"/>
                        <TextBlock Text="Set as Avatar" VerticalAlignment="Center"/>
                    </StackPanel>
                </ui:Button>
            </StackPanel>
            <custom:LoadingSpinner x:Name="LoadingRing"
                Cap="Round"
                Diameter="96"
                IsLoading="true"
                Thickness="12"
                Color="#5E5EFF" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Center"/>
        </Grid>
        <ContentPresenter x:Name="RootContentDialogPresenter"/>
    </Grid>
</ui:FluentWindow>